[project]
name = "logiwa-shipments"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "dotenv",
    "pymssql",
    "aiohttp>=3.13.1",
]
